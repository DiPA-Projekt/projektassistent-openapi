ProjectTypeVariant:
  type: object
  properties:
    id:
      description: Unique identifier of the project type variant
      type: string
      example: "da34fa7cd44399"
    name:
      description: Name of the project type variant
      type: string
      example: "AG-Projekt mit einem Auftragnehmer"
  required:
    - id
    - name
